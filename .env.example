# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# AI / Gemini
VITE_GEMINI_API_KEY=

# Worker proxy (Cloudflare Workers / local dev)
# In produzione punta al dominio del worker; in dev resta vuoto per usare http://localhost:5000
VITE_WORKER_URL=

# CORS: aggiorna ALLOWED_ORIGINS in worker.js con il dominio Pages/Workers finale
